extends ../layout

block content
    h1.
        #{title}
    form#formAddpost(name="addpost",method="post",action="/posts",enctype='application/x-www-form-urlencoded')
        p Title:
            input#inputName(type="text", name="title")
        p Body:
            textarea#inputName(type="text", name="body")
        p
            button#btnSubmit(type="submit") submit